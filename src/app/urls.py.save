from django.urls import path
from django.views.decorators.csrf import csrf_exempt

from app.views import BlogHome, BlogHome1, BlogHome2, BlogPostDetail, BlogPostView, BlogPostCreate, BlogPostDelete, \
    BlogPostUpdate, BlogPostCreate1, BlogPostUpdate1, BlogPostDelete1, admin_count

app_name = 'app'



urlpatterns = [
    path('', BlogHome.as_view(), name='home'),
   # path('create/', BlogPostCreate.as_view(), name='create'),
    path('create1/', BlogPostCreate1.as_view(), name='create1'),
    path('edit/<str:slug>/', BlogPostUpdate.as_view(), name='edit'),
    path('edit1/<str:slug>/', BlogPostUpdate1.as_view(), name='edit1'),
    path('edit/', BlogHome1.as_view(), name='update'),
    path('detail/', BlogHome2.as_view(), name='det'),
    path('<str:slug>/', BlogPostDetail.as_view(), name='detail'),
    path('view/<str:slug>/', BlogPostView.as_view(), name='update'),
    path('delete/<str:slug>/', BlogPostDelete.as_view(), name='delete'),
    path('delete1/<str:slug>/', BlogPostDelete1.as_view(), name='delete1'),
    path('admin_count/', admin_count, name='admin-count'),
]/usr/local/bin/python3 /Users/sadiobah/Downloads/kafo_inventory/src/app/urls.py

